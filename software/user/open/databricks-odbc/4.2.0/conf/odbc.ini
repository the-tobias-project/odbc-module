[Databricks]
Driver=Databricks Driver
ErrorMessagesPath=${SPARKPATH}/ErrorMessages
Host=${DATABRICKS_HOSTNAME}
Port=${DATABRICKS_PORT}
HTTPPath=${DATABRICKS_HTTP_PATH}
ThriftTransport=2
SSL=1
AuthMech=3
UID=token
PWD=${DATABRICKS_TOKEN}